# RETURN — Hackathon Progress

## Aaryan
- [14:42 PT] Set up Doppler (project: berkeley-hackathon, config: dev) for team-wide OPENROUTER_API_KEY sharing; connected repo aaryan-rampal/scaling-octo-fortnight to GitHub and pushed skills + current.md; added teammates drkchu, selinmutlu06, n1sak with write access after pulling GitHub usernames from team Slack DM; set up Slack MCP server for Claude Code; researched Hindsight (vectorize-io/hindsight) — confirmed it runs as an HTTP service on :8888 with embedded Postgres (pg0, no Docker needed), already wired to OpenRouter in hindsight-setup; pivoted plan to run Hindsight fully self-contained inside recall/ in a git worktree; explored Docker + Colima setup on external SSD (Samsung Extreme SSD, exFAT) for VM storage; now actively testing Hindsight integration
- [raw→principles vertical, 2026-06-21] Verified the full `recall.db` rebuild (270,969 events across imessage/spotify/photos/claude; spotify back to ~123.6k after the min_ms_played fix; photo vision = 28 local-thumbnail descriptions; imessage contact names + spotify vibes ~all on the 30d slice). Added live loguru observability to the build (`src/core/logging.py`) after it ran dark for 10min — and to `scripts/retain_slice.py` after a sequential retain run burned ~$11 (OpenRouter $4→$15) showing ZERO visible progress (cause: `print()` block-buffered to a file; fix: loguru→stderr, must capture with `2>&1`). KEY LEARNING: long runs must background + log to a file (now a CLAUDE.md convention). The killed retain actually committed ~1010 memories from ~159 of 403 units before I stopped it — so bank `slice-7d` now holds 1010 memories (claude-skewed: claude 838 / imessage 87 / spotify 53 / photos 32). Built + verified `scripts/dump_bank.py` → `data/bank_snapshot.json`: 1010/1010 memories trace back to raw events (100% provenance), 9-field contract intact. Cracked the NO-RE-EMBED path: Hindsight's qwen vectors (2000-dim) read straight from pg0 (`postgresql://hindsight:hindsight@127.0.0.1:5432/hindsight`, table `memory_units` col `embedding`, key by id) — `recall()` doesn't expose them. Found clustering threshold 0.78 (engine default 0.55 single-link-chains 205/245 memories into one blob). Now parallelizing with Sonnet subagents: `propose.py`+`mint_principles.py` (live PrincipleProposer, observable-before-paid) and an EDA refresh/compare. Full mint run still GATED on go (paid). All work uncommitted, NOT pushed.

## Derek

## Nisa

## Selin
- [principle trace-back + "why" agent, 2026-06-21] Built the read path that surfaces a principle's provenance and explains it. `src/storage/trace.py` walks the SQLite ladder (principle → principle_memories → memories → memory_events → events) into nested dataclasses — read-only connection (`mode=ro`, so a wrong `--db` fails loudly instead of creating an empty DB), dedupes events across duplicate join rows, tolerates null confidence / thin (event-less) memories. `src/pipeline/explain.py` adds the LLM "why" agent behind an injectable `WhyExplainer` seam (live `LLMWhyExplainer` reuses the OpenRouter/openai pattern from `link.py`; grounded-only, conversational, forward-looking per CLAUDE.md §1). Wired `recall explain [id]` into the CLI (lists ids when none given; `--db`, `--json`; friendly errors for missing DB / missing key). 12 offline fixture-based tests (dedupe, null-confidence, read-only, not-found), full suite 276 green, ruff clean. Derek's handoff arrived as `derek_sample.json` (not the .db) — `scripts/load_sample_json.py` rebuilds `data/derek_handoff/derek_sample.db` from it (same SCHEMA.md schema). Verified end-to-end on the real slice: tracing the hub principle returns 2 memories / 44 raw events with real content + raw_refs. Live LLM call still gated on Doppler access (my CLI token is in the wrong workplace — `recapture`, not `berkeley-hackathon`; needs a `dev` service token). Branch `selin-principle-traceback`. Design doc: `docs/principle-traceback-agent.md`.
